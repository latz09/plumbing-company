const OpeningCards = () => {
    return ( <div>
        {data.map((item, index) => (
            <div key={index}>
                
            </div>
        ))}
    </div> );
}
 
export default OpeningCards;